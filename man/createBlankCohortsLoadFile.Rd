% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest_cohorts.R
\name{createBlankCohortsLoadFile}
\alias{createBlankCohortsLoadFile}
\title{Create Blank Cohorts Load File}
\usage{
createBlankCohortsLoadFile(cohortsFolderPath = here::here("inputs/cohorts"))
}
\arguments{
\item{cohortsFolderPath}{Character. Path where the blank file will be created.
Defaults to "inputs/cohorts". Creates the folder if it doesn't exist.}
}
\value{
Invisibly returns the file path. Prints informative messages with tips.
}
\description{
Creates a blank cohortsLoad.csv template file in the specified folder
with proper column structure. Users can fill this file manually in Excel,
Google Sheets, or any text editor, then place it in the inputs/cohorts folder.
}
\details{
\strong{Column Guide:}
\itemize{
\item \code{atlasId} (numeric): The ATLAS cohort ID. Get this from ATLAS > Cohort Definitions
\item \code{label} (character): Display name for your cohort (e.g., "Type 2 Diabetes patients")
\item \code{category} (character): Broad grouping category (e.g., "Disease Populations", "Treatment Groups")
\item \code{subCategory} (character): Optional sub-grouping within category
\item \code{file_name} (character): Path to JSON file (e.g., "json/t2dm_patients.json"). Note this is a placeholder will be replaced when you import from ATLAS.
}

\strong{Tips for Filling Out:}
\enumerate{
\item Each row represents one cohort
\item Use forward slashes (/) in file paths
\item Ensure file_name matches the JSON files you'll import from ATLAS
\item Logical sub-grouping in category/subCategory helps with organization
\item Save as UTF-8 CSV when exporting from Excel to avoid encoding issues
}

\strong{Workflow:}
\enumerate{
\item Call this function to create blank template
\item Open cohortsLoad.csv in your preferred spreadsheet application
\item Fill in your cohort metadata
\item Save the file
\item Use \code{\link[=importAtlasCohorts]{importAtlasCohorts()}} to import the actual JSON definitions from ATLAS
\item Use \code{\link[=loadCohortManifest]{loadCohortManifest()}} to load into your study
}
}
\examples{
\dontrun{
  # Create blank template in default location
  createBlankCohortsLoadFile()
  # File created at: inputs/cohorts/cohortsLoad.csv
}

}
