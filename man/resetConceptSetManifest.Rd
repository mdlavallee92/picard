% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest_conceptSets.R
\name{resetConceptSetManifest}
\alias{resetConceptSetManifest}
\title{Reset Concept Set Manifest Database}
\usage{
resetConceptSetManifest(
  conceptSetsFolderPath = here::here("inputs/conceptSets")
)
}
\arguments{
\item{conceptSetsFolderPath}{Character. Path to the conceptSets folder containing the manifest
database. Defaults to "inputs/conceptSets".}
}
\value{
Invisibly returns NULL. Deletes the manifest file and prints status messages.
}
\description{
Deletes the conceptSetManifest.sqlite database file. Use this function when you need
to reset the manifest and rebuild it from the available concept set files.
}
\details{
This function is useful for:
\itemize{
\item Starting fresh with a new set of concept sets
\item Clearing cached manifest data
\item Resolving manifest corruption issues
}

After resetting, call \code{\link[=loadConceptSetManifest]{loadConceptSetManifest()}} to rebuild the manifest from
the available concept set files in the json/ subdirectory.
}
\examples{
\dontrun{
  # Reset the manifest
  resetConceptSetManifest()

  # Rebuild it from concept set files
  settings <- ExecutionSettings$new(...)
  manifest <- loadConceptSetManifest(settings)
}

}
