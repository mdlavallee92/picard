% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest_conceptSets.R
\name{importAtlasConceptSets}
\alias{importAtlasConceptSets}
\title{Import CIRCE Concept Sets from ATLAS}
\usage{
importAtlasConceptSets(
  conceptSetsFolderPath = here::here("inputs/conceptSets"),
  atlasConnection
)
}
\arguments{
\item{conceptSetsFolderPath}{Character. Path to conceptSets folder in the project.}

\item{atlasConnection}{An ATLAS connection object (typically from ROhdsiWebApi
package) with a method \code{getConceptSetDefinition(conceptSetId)} that returns
a list containing an \code{expression} element with the CIRCE JSON string.}

\item{outputFolder}{Character. Path to the output folder where concept set JSON
files will be saved. Defaults to inputs/conceptSets/json. Files are saved as
\verb{\{label\}.json}.}
}
\value{
Invisibly returns the updated concept set load dataframe. Saves CIRCE
JSON files to outputFolder and prints status messages via cli alerts.
}
\description{
This function looks for a CSV file called conceptSetsLoad.csv
containing concept set metadata. Must be located in or accessible from the
inputs/conceptSets folder. The CSV must have the following columns:
\itemize{
\item \code{atlasId}: ATLAS concept set definition ID (integer)
\item \code{label}: Concept set name/label (character)
\item \code{domain}: OMOP domain (drug_exposure, condition_occurrence, measurement, procedure)
\item \code{sourceCode}: Whether the concept set represents source codes (logical)
}

The function will read this CSV, fetch the concept set definitions from ATLAS
using the provided atlasConnection, extract the CIRCE JSON expressions, and
save them to the specified output folder with filenames based on the label.
Finally it updates the concept set load CSV with the relative file paths to
the saved JSON files.
}
\details{
Imports CIRCE JSON concept set definitions from an ATLAS WebAPI instance and
saves them to the inputs/conceptSets/json folder. This function reads a CSV file
containing concept set metadata and fetches the actual concept set definitions
from ATLAS.

\strong{Workflow:}
\enumerate{
\item Reads the concept set load CSV file
\item Validates that all required columns are present
\item For each row with a valid atlasId:
\itemize{
\item Fetches the concept set definition from ATLAS WebAPI
\item Extracts the CIRCE JSON expression
\item Saves to \verb{outputFolder/\{label\}.json}
}
\item Skips rows with missing atlasId with a warning
\item Catches and reports errors per concept set without stopping the entire import
}

\strong{Post-Import:}
After running this function, use \code{\link[=loadConceptSetManifest]{loadConceptSetManifest()}} to load the saved
concept set JSON files and build the manifest with metadata.
}
\examples{
\dontrun{
  # Assuming ATLAS connection is set up
  importAtlasConceptSets(
    conceptSetsFolderPath = here::here("inputs/conceptSets"),
    atlasConnection = setAtlasConnection()
  )

  # Then load the manifest
  manifest <- loadConceptSetManifest(
    conceptSetsFolderPath = here::here("inputs/conceptSets")
  )
}

}
