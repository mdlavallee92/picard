% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/buildDependentCohorts.R
\name{buildSubsetCohortDemographic}
\alias{buildSubsetCohortDemographic}
\title{Build a Subset Cohort Definition (Demographic)}
\usage{
buildSubsetCohortDemographic(
  label,
  baseCohortId,
  minAge = NULL,
  maxAge = NULL,
  genderConceptIds = NULL,
  raceConceptIds = NULL,
  ethnicityConceptIds = NULL,
  cohortsDirectory = NULL,
  manifest = NULL
)
}
\arguments{
\item{label}{Character. User-friendly name for the subset (e.g., "CKD - Males 40-75")}

\item{baseCohortId}{Integer. The cohort ID to subset.}

\item{minAge}{Integer. Minimum age at cohort start. NULL = no minimum. Default: NULL}

\item{maxAge}{Integer. Maximum age at cohort start. NULL = no maximum. Default: NULL}

\item{genderConceptIds}{Numeric vector. Gender concept IDs to include. NULL = all. Default: NULL}

\item{raceConceptIds}{Numeric vector. Race concept IDs to include. NULL = all. Default: NULL}

\item{ethnicityConceptIds}{Numeric vector. Ethnicity concept IDs to include. NULL = all. Default: NULL}

\item{cohortsDirectory}{Character. Path to inputs/cohorts/. Uses study hierarchy if not provided.}

\item{manifest}{CohortManifest object (optional). If provided, validates that base cohort exists.
Recommended to ensure referential integrity. If NULL, a warning is issued.}
}
\value{
A CohortDef object with cohortType='subset' and dependencies set.
}
\description{
Creates a SQL file and metadata for a subset cohort based on person-level demographics.
Returns a CohortDef object ready to add to a CohortManifest.
}
\details{
Creates three files:
\itemize{
\item SQL file: \verb{inputs/cohorts/derived/subset_demo/subset_demo_cohort_\{baseCohortId\}.sql}
\item Metadata JSON: Same path with \code{.json} extension
\item Context file: \code{.metadata} with filter description
}
}
