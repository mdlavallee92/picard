% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/taskTracking.R
\name{shouldRerunTask}
\alias{shouldRerunTask}
\title{Check if Task Needs to be Rerun}
\usage{
shouldRerunTask(
  taskFile,
  configBlock,
  executionSettings,
  pipelineVersion,
  tasksFolderPath = here::here("analysis/tasks")
)
}
\arguments{
\item{taskFile}{Character. Name or path of the task file (e.g., "task1.R")}

\item{configBlock}{Character. The config block name (e.g., "optum_dod")}

\item{executionSettings}{ExecutionSettings object}

\item{pipelineVersion}{Character. Current pipeline version (e.g., "1.0.0")}

\item{tasksFolderPath}{Character. Path to tasks folder (default: here::here("analysis/tasks"))}
}
\value{
List with elements:
\itemize{
\item should_rerun: Logical. TRUE if task should be rerun
\item reasons: Character vector. Why task should be rerun
\item last_run_info: List with previous run details (time, version, status)
\item task_file_hash: Current hash of task file
\item cohort_hash_status: List comparing manifest hashes to database hashes
}
}
\description{
Determines whether a task needs to be rerun by checking:
\enumerate{
\item Task file modifications (file hash comparison)
\item Dependency file modifications (extracted from source() calls)
\item Cohort generation changes (hash comparison in database)
\item Previous run errors (checked in logs and history)
\item Version changes
}
}
\details{
Creates/updates exec/logs/task_run_history.csv tracking:
\itemize{
\item task_name, config_block, last_run_time, pipeline_version
\item task_file_hash, cohort_hash_match, status, error_message
}
}
