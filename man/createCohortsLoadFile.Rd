% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest.R
\name{createCohortsLoadFile}
\alias{createCohortsLoadFile}
\title{Interactively Create Cohort Load File}
\usage{
createCohortsLoadFile(cohortsFolderPath = here::here("inputs/cohorts"))
}
\arguments{
\item{cohortsFolderPath}{Character. Path to the cohorts folder where cohortsLoad.csv
will be saved. Defaults to "inputs/cohorts".}
}
\value{
Invisibly returns a data frame containing the cohort load metadata.
Saves the file to \code{cohortsFolderPath/cohortsLoad.csv}.
}
\description{
Guides the user through an interactive process to create a cohortsLoad.csv file.
The file contains metadata about cohorts to be imported from ATLAS and is used by
\code{\link[=importAtlasCohorts]{importAtlasCohorts()}} to fetch and save cohort definitions.
}
\details{
The function prompts the user to enter information for each cohort:
\itemize{
\item \strong{atlasId}: ATLAS cohort definition ID (numeric)
\item \strong{label}: Human-readable name for the cohort (character)
\item \strong{category}: Broad category/classification (character)
\item \strong{subCategory}: More specific sub-category (character)
}

After each cohort entry, the user is asked whether to add another cohort.
When complete, the data is saved to \code{cohortsLoad.csv} in the cohorts folder.

\strong{Workflow:}
\enumerate{
\item Call this function to interactively create the load file
\item Use \code{\link[=importAtlasCohorts]{importAtlasCohorts()}} to import cohorts from ATLAS using this file
\item Use \code{\link[=loadCohortManifest]{loadCohortManifest()}} to load the imported cohorts and build the manifest
}
}
\examples{
\dontrun{
  # Create the load file interactively
  createCohortsLoadFile()

  # Then import cohorts from ATLAS
  importAtlasCohorts(
    cohortLoadPath = "inputs/cohorts/cohortsLoad.csv",
    atlasConnection = atlas_conn
  )
}

}
