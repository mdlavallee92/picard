% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/postProcess.R
\name{reviewExportSchema}
\alias{reviewExportSchema}
\title{Review Export File Schema}
\usage{
reviewExportSchema(exportPath = here::here("dissemination/export/merge"))
}
\arguments{
\item{exportPath}{Character. Path to the export folder containing merged results.
Defaults to "dissemination/export/merge"}
}
\value{
Data frame with columns:
\itemize{
\item fileName: Name of the CSV file
\item columnName: Name of the column
\item dataType: R data type as detected by readr (character, numeric, logical, etc.)
\item rowCount: Number of rows in the file
}
}
\description{
Examines all CSV files in the export folder and extracts schema information
(column names and data types). Useful for identifying ETL requirements before dissemination.
}
\details{
This function helps identify:
\itemize{
\item Column naming inconsistencies across files
\item Unexpected data types that may need transformation
\item Columns that should be renamed or restructured
\item Data quality issues (e.g., columns with mostly NAs)
}

The data frame can be sorted/filtered to understand transformation requirements.
}
\examples{
\dontrun{
  schema <- reviewExportSchema()
  
  # View all columns and types
  print(schema)
  
  # Check for character columns that should be numeric
  schema[schema$dataType == "character", ]
  
  # Get distinct data types per file
  schema |>
    dplyr::group_by(fileName) |>
    dplyr::summarise(colCount = dplyr::n(), .groups = "drop")
}
}
