% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest_cohorts.R
\name{visualizeCohortDependencies}
\alias{visualizeCohortDependencies}
\title{Visualize Cohort Dependencies in a Report}
\usage{
visualizeCohortDependencies(manifest, outputPath = NULL)
}
\arguments{
\item{manifest}{A CohortManifest object containing loaded cohorts.}

\item{outputPath}{Character. Optional path to save the markdown report. If NULL,
the report is not saved to file. If a folder path is provided, the report is
saved as "cohort_dependencies.md" in that folder. Defaults to NULL.}
}
\value{
Character. The markdown report content (invisibly if saved to file).
}
\description{
Creates a comprehensive markdown report visualizing the dependency structure
of all cohorts in a CohortManifest. The report includes a mermaid diagram
showing the dependency graph and a detailed table of all cohorts with their
relationships.
}
\details{
The report includes:
\itemize{
\item \strong{Overview}: Summary statistics (total cohorts, base cohorts, dependent cohorts)
\item \strong{Dependency Diagram}: Mermaid graph showing how cohorts depend on each other
\item \strong{Cohort Summary Table}: Details on each cohort including type and dependencies
\item \strong{Dependency Tree}: Hierarchical view of base cohorts and their dependents
}

The mermaid diagram uses:
\itemize{
\item Rectangles for CIRCE (base) cohorts
\item Circles for subset cohorts
\item Diamonds for union cohorts
\item Hexagons for complement cohorts
\item Arrows showing dependency direction (parent → dependent)
}
}
\examples{
\dontrun{
  manifest <- loadCohortManifest()
  
  # View report in console
  report <- visualizeCohortDependencies(manifest)
  
  # Save report to cohorts folder
  visualizeCohortDependencies(manifest, outputPath = "inputs/cohorts")
}

}
