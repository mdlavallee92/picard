% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/buildDependentCohorts.R
\name{buildCompositeCohort}
\alias{buildCompositeCohort}
\title{Build a Composite Cohort Definition}
\usage{
buildCompositeCohort(
  label,
  criteriaCohortIds,
  minimumEventCount = 1,
  eventSelection = "First",
  cohortsFolder = here::here("inputs/cohorts"),
  manifest = NULL
)
}
\arguments{
\item{label}{Character. User-friendly name for the composite (e.g., "Diabetes mellitus").}

\item{criteriaCohortIds}{Integer vector. The cohort IDs to include in the composite
(e.g., c(1, 2, 3) for Type 1 diabetes, Type 2 diabetes, and secondary diabetes).}

\item{minimumEventCount}{Integer. Minimum number of distinct cohort events required for a subject
to qualify for the composite. Default: 1 (any subject with at least 1 event qualifies).}

\item{eventSelection}{Character. One of 'First', 'Last', or 'All'. Specifies which event(s) to
retain as the cohort_start_date and cohort_end_date in the output:
\itemize{
\item 'First': Keep the earliest event (earliest index date)
\item 'Last': Keep the most recent event
\item 'All': Keep all qualifying events per subject (may result in multiple rows per subject)
Default: 'First'.
}}

\item{manifest}{CohortManifest object (optional). If provided, validates that all criteria cohorts exist.
Recommended to ensure referential integrity. If NULL, a warning is issued.}

\item{cohortsDirectory}{Character. Path to inputs/cohorts/. Uses study hierarchy if not provided.}
}
\value{
A CohortDef object with cohortType='composite' and dependencies set.
}
\description{
Creates a SQL file and metadata for a composite cohort that combines multiple cohort definitions.
A composite cohort groups subjects who have at least N qualifying events from a set of cohort definitions.
Returns a CohortDef object ready to add to a CohortManifest.
}
\details{
Creates three files:
\itemize{
\item SQL file: \verb{inputs/cohorts/derived/composite/composite_cohort_\{hash\}.sql}
\item Metadata JSON: Same path with \code{.json} extension (parameters for execution)
\item Hash ensures uniqueness when same criteria are used with different labels
}
}
\examples{
# Create a composite cohort for diabetes (any type): Type 1, Type 2, or secondary diabetes
# Keep only subjects with at least 1 event (any diagnosis), using first event as index date

diabetes_cohort <- buildCompositeCohort(
  label = "Diabetes mellitus (any type)",
  criteriaCohortIds = c(101, 102, 103),
  minimumEventCount = 1,
  eventSelection = "First"
)

}
