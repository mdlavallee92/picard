% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest.R
\name{loadCohortManifest}
\alias{loadCohortManifest}
\title{Load Cohort Manifest from Database or Cohort Files}
\usage{
loadCohortManifest(
  executionSettings,
  cohortsFolderPath = here::here("inputs/cohorts")
)
}
\arguments{
\item{executionSettings}{An ExecutionSettings object containing database configuration
for cohort generation.}

\item{cohortsFolderPath}{Character. Path to the cohorts folder containing the manifest
database and cohort definition files. Defaults to "inputs/cohorts". The function
will look for:
\itemize{
\item \code{cohortManifest.sqlite} in this folder for existing manifest data
\item \verb{json/} subfolder for CIRCE JSON cohort definitions
\item \verb{sql/} subfolder for SQL cohort definitions
}}
}
\value{
A CohortManifest R6 object initialized with all cohorts found.
}
\description{
Loads a CohortManifest R6 object by either reading from an existing
cohortManifest.sqlite database or by scanning the inputs/cohorts directories.
}
\details{
\strong{If database exists:}
Loads cohort paths and metadata from the cohortManifest.sqlite database,
verifies files still exist, and checks if any files have changed by comparing
the stored hash with the current file hash.

\strong{If database doesn't exist:}
Scans \code{cohortsFolderPath/json} and \code{cohortsFolderPath/sql} directories to find cohort
definition files and creates a new CohortEntry for each file with:
\itemize{
\item label: The basename of the file without extension
\item tags: Empty list
\item filePath: The full path to the cohort file
}

\strong{Metadata Enrichment (optional):}
If a \code{cohortsLoad.csv} file exists in \code{cohortsFolderPath}, the function will
automatically enrich CohortEntry objects with tags by matching the \code{file_name}
column from the load file with the \code{filePath} of each entry. For matching entries,
tags are added from the following columns:
\itemize{
\item \code{atlasId}: Added as an "atlasId" tag
\item \code{category}: Added as a "category" tag
\item \code{subCategory}: Added as a "subCategory" tag
}

Hash comparison alerts:
\itemize{
\item \strong{✓ Unchanged}: Hash matches stored value
\item \strong{⚠ Changed}: Hash differs from stored value (file was modified)
}
}
\examples{
\dontrun{
  settings <- ExecutionSettings$new(
    databaseName = "mydb",
    dbms = "postgresql",
    connectionDetails = list(...)
  )
  manifest <- loadCohortManifest(settings, cohortsFolderPath = "path/to/cohorts")
}

}
