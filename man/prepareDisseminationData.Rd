% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/disseminate.R
\name{prepareDisseminationData}
\alias{prepareDisseminationData}
\title{Prepare Dissemination Data with Chained Transformations}
\usage{
prepareDisseminationData(
  data,
  clean_names = TRUE,
  format_percentages = TRUE,
  format_floats = TRUE,
  standardize_types = TRUE,
  percent_decimal_places = 1,
  float_decimal_places = 2
)
}
\arguments{
\item{data}{Data frame or tibble to prepare}

\item{clean_names}{Logical. Apply cleanColumnNames(). Defaults to TRUE.}

\item{format_percentages}{Logical. Apply formatPercentages(). Defaults to TRUE.}

\item{format_floats}{Logical. Apply formatFloats(). Defaults to TRUE.}

\item{standardize_types}{Logical. Apply standardizeDataTypes(). Defaults to TRUE.}

\item{percent_decimal_places}{Integer. Decimal places for percentages. Defaults to 1.}

\item{float_decimal_places}{Integer. Decimal places for floats. Defaults to 2.}
}
\value{
Data frame prepared for dissemination
}
\description{
Convenience function that chains common data preparation steps
for dissemination: cleaning names, formatting numbers, standardizing types.
}
\details{
This function applies transformations in sequence:
\enumerate{
\item Clean column names to snake_case
\item Format percentage columns
\item Format float columns
\item Standardize data types
}

Each step is optional and can be controlled individually.
}
