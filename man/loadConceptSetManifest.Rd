% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest_conceptSets.R
\name{loadConceptSetManifest}
\alias{loadConceptSetManifest}
\title{Load Concept Set Manifest}
\usage{
loadConceptSetManifest(
  conceptSetsFolderPath = here::here("inputs/conceptSets"),
  executionSettings = NULL
)
}
\arguments{
\item{conceptSetsFolderPath}{Character. Path to the conceptSets folder containing the manifest
database. Defaults to "inputs/conceptSets".}

\item{executionSettings}{ExecutionSettings object. Optional. Defaults to NULL.
Only required for operations like extractSourceCodes(). You can add settings later
using setExecutionSettings() on the returned ConceptSetManifest object.}
}
\value{
ConceptSetManifest object containing all loaded concept sets with metadata.
}
\description{
Loads or creates a concept set manifest from CIRCE JSON files located in the
inputs/conceptSets/json folder. The manifest is stored in an SQLite database
for efficient querying and metadata persistence. ExecutionSettings are optional
and only required if you plan to extract source codes or access vocabularies.
}
\details{
\strong{Workflow:}
\enumerate{
\item Checks if conceptSetManifest.sqlite database exists
\item If it exists, loads concept set entries from the json/ directory using cached metadata
\item If not, scans the json/ directory for CIRCE JSON files
\item Creates ConceptSetDef objects for each JSON file
\item Enriches metadata from conceptSetsLoad.csv if available
\item Returns a ConceptSetManifest object
}

\strong{Metadata CSV Format:}
The conceptSetsLoad.csv file (optional) should contain:
\itemize{
\item \code{file_name}: Relative path to JSON file (e.g., "conceptSet1.json")
\item \code{label}: Display name for the concept set
\item \code{atlasId}: ATLAS concept set ID
\item \code{domain}: OMOP domain classification
\item \code{sourceCode}: Whether the concept set represents source codes
}

\strong{Post-Load:}
After loading, use manifest methods to query concept sets:
\itemize{
\item \code{queryConceptSetsByIds(ids)} - Query by one or more IDs; returns data frame
\item \code{queryConceptSetsByTag(tagStrings, match)} - Query by tag(s); returns data frame
\item \code{queryConceptSetsByLabel(labels, matchType)} - Query by label(s); returns data frame
\item \code{getConceptSetById(id)} - Get ConceptSetDef object by ID
\item \code{getConceptSetsByTag(tagStrings, match)} - Get ConceptSetDef objects by tag(s)
\item \code{tabulateManifest()} - Tabular view of all concept sets
}
}
\examples{
\dontrun{
  # Load concept set manifest (no settings required for metadata review)
  manifest <- loadConceptSetManifest()
  
  # Or load from custom path
  manifest <- loadConceptSetManifest(conceptSetsFolderPath = "path/to/conceptsets")
  
  # Add execution settings later if needed for source code extraction
  settings <- createExecutionSettings(
    connectionString = "Server=localhost;Database=mydb"
  )
  manifest$setExecutionSettings(settings)
  manifest$extractSourceCodes(sourceVocabs = c("ICD10CM"))
}

}
